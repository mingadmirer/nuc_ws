# WUST_RC 2026ROBOCON 上位机决策和控制接口层

## 上下位机接口层
与电控组协同工作，实现上位机与下位机的通信和控制。
定义串口帧格式，包括数据帧和控制帧。
将上位机的决策指令转换为下位机的控制信号，限制极值，包含对边界状态的保护。

## 上位机决策层
主要的感知模块为深度相机，经yolo训练后，以及内外参的校准，实现对目标的检测和深度定位。
依据状态机。
根据当前状态和目标位置，判断是否需要改变状态，做出相应的决策。